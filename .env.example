# Neon Postgres Database Connection
# Use a read-only connection string if available
DATABASE_URL=\
postgresql://user:password@host:5432/database?sslmode=require\

# API Keys (comma-separated for key rotation support)
# Example: BIOMECH_API_KEYS=\key1
key2
key3\
BIOMECH_API_KEYS=\your-api-key-here\

# Octane app integration (user lookup by email, app_db_uuid resolution)
# Base URL of the Octane app. Use HTTPS in production.
# For local testing: use your local Octane app URL (e.g. http://localhost:3001).
# When OCTANE_APP_API_URL is localhost in development, OCTANE_API_KEY is optional.
OCTANE_APP_API_URL=\
# Secret for Authorization: Bearer ... Never commit or expose; server-side only. Optional when testing against localhost in dev.
OCTANE_API_KEY=\
